apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: my-ingress-istio
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Exact
            backend:
              service:
                name: istio-ingressgateway
                port: 
                  number: 80